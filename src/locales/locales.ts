import { DefaultLocale } from './default-locale';

const en: DefaultLocale = {
    hello: 'hello',
    login: 'login',
};

const ru: DefaultLocale = {
    hello: 'привет',
    login: 'логин',
};

export { en, ru };
